SANITY_PROJECT_ID=nyd3p2n0
SANITY_DATASET=production
SANITY_API_TOKEN=your-read-api-token

# Only needed to upload image assets via `make upload-image`. The site builds
# and runs without it, so it stays commented out: upload-image.sh guards on it
# and prints how to create one, and a placeholder value here would replace that
# message with an opaque 401 from the API instead.
# SANITY_WRITE_TOKEN=your-editor-token
